{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "live-server public --port=8080",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "live-server": "^1.2.2"
  },
  "jest": {
    "testEnvironment": "jest-environment-jsdom"
  }
}
